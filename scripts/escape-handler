#!/bin/bash
DIR="$(dirname "$0")/../widgets"
ACTIVE=$(eww active-windows 2>/dev/null)

for widget in "$DIR"/*/; do
    name=$(basename "$widget")
    [ "$name" = "bar" ] && continue
    echo "$ACTIVE" | grep -q "$name" && eww close "$name"
done
